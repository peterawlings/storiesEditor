<h1>Stories</h1>
<div class="row">
  {{#each stories}}
  <div class="col s12 m4">
    <div class="card">
      <div class="card-image">
        {{!-- Triple curlies to parse HTML --}}
        {{{editIcon user ../user _id }}} {{!-- '../user' is a way to break out of the loop and get the logged in user,
        not
        'user' in 'stories' - also need to set it as an Express global variable so it's accessible here--}}

      </div>
      <div class="card-content center-align">
        <h5>{{title}}</h5>
        <p>{{stripTags (truncate body 150)}}</p> {{!-- The way to use mutiple helpers --}}
        <br>
        <div class="chip">
          <img src="{{user.image}}" alt="">
          <a href="/stories/user/{{user._id}}">{{user.displayName}}</a>
        </div>
      </div>
      <div class="card-action center-align">
        <a href="/stories/{{_id}}" class="btn grey">Read More</a>
      </div>
    </div>
  </div>
  {{else}}
  <p>No stories to display</p>
  {{/each}}

</div>